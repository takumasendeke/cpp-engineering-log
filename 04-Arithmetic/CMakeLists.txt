# Create three separate executables. 
# Give them clear, distinct names so you know which is which in VS Code!

add_executable(arithmetic_ops arithmetic/main.cpp)
add_executable(math_funcs math_functions/main.cpp)
